/*-- Altes Zeug, das nicht mehr in die Engine muss --*/

#strict

// Fügt das Material in ein Fass im Objekt ein
global func GetBarrelType(int imat) {
  return(1);
}

global func BarrelDoFill(int imat, int imat) {
  return(1);
}